import { Image } from './image';

export class Vila {
    name: string;
    width: number;
    height: number;
    area: number;
    address: string;
    images: Image[];
    province: {name: string};
}
